CREATE TABLE tablename (
no1 TINYINT,
no2 SMALLINT,
no3 INT,
no4 BIGINT,
no5 FLOAT,
no6 DECIMAL(5,4),
name STRING,
name2 VARCHAR(30),
phone_numbers ARRAY<BIGINT>,
deductions MAP<CHAR, FLOAT>,
address   STRUCT<street:STRING, city:STRING, state:STRING, zip:INT>,
others    UNIONTYPE<FLOAT,BOOLEAN,STRING> )
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\001'
COLLECTION ITEMS TERMINATED BY '\002'
MAP KEYS TERMINATED BY '\003'
LINES TERMINATED BY '\n';
STORED AS TEXTFILE;

LOAD DATA LOCAL INPATH 'home/cloudera/filename' INTO TABLE tablename;

CREATE EXTERNAL TABLE tablename (
no1 TINYINT,
no2 SMALLINT,
no3 INT,
no4 BIGINT,
no5 FLOAT,
no6 DECIMAL(5,4),
name STRING,
name2 VARCHAR(30),
phone_numbers ARRAY<BIGINT>,
deductions MAP<CHAR, FLOAT>,
address   STRUCT<street:STRING, city:STRING, state:STRING, zip:INT>,
others    UNIONTYPE<FLOAT,BOOLEAN,STRING> )
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\001'
COLLECTION ITEMS TERMINATED BY '\002'
MAP KEYS TERMINATED BY '\003'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '/user/hive/warehouse/filename';

LOAD DATA INPATH '/user/hive/warehouse/filename' INTO TABLE tablename;
